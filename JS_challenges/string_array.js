function stringToArray(str) {
    return str.split('');
}

// Example usage:
let inputString = "Hello";
let charArray = stringToArray(inputString);
console.log("Array of characters:", charArray);
